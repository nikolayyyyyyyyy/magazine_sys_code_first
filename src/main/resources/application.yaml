spring:
  datasource:
    url: jdbc:mysql://localhost:3306/magazine_sys?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
      format_sql: true
      dialect: org.hibernate.dialect.MySQLDialect